<p id="notice"><%= notice %></p>

<h2>Your Pragmatic Cart</h2>

<ul>

	<% for item in @cart.line_items %>

		<li><%= item.quantity rescue item %> &times; <%= item.product.title %></li>

	<% end %>

</ul>

<%= button_to 'Empty cart', @cart, :method => :delete, :confirm => 'Are you sure?' %>
